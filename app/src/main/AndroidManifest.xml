<?xml version="1.0" encoding="utf-8"?>
<!--
  AndroidManifest.xml — патч-инструкция
  ======================================
  Применить вручную: найти и удалить/добавить отмеченные блоки.

  ── УДАЛИТЬ (блок RegLicenseActivity) ──────────────────────────────────────
  <activity
      android:name="org.pniei.moduleskzi.activities.RegLicenseActivity"
      android:screenOrientation="portrait"
      android:exported="true">
  </activity>

  ── ДОБАВИТЬ (разрешение USB для OTG-кардридера) ───────────────────────────
  Добавить в <manifest> рядом с остальными uses-permission:

  <uses-feature android:name="android.hardware.usb.host" android:required="false"/>
  <uses-permission android:name="android.permission.USB_PERMISSION"/>

  ── ДОБАВИТЬ (USB intent-filter для автоматического запроса разрешения) ────
  Добавить в <activity android:name=".activities.LauncherActivity">:

  <intent-filter>
      <action android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED"/>
  </intent-filter>
  <meta-data
      android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED"
      android:resource="@xml/usb_device_filter"/>

  ── СОЗДАТЬ res/xml/usb_device_filter.xml ──────────────────────────────────
  (Фильтр для mass-storage устройств. Без него UsbManager не попросит
   разрешение автоматически при вставке карты.)

  <?xml version="1.0" encoding="utf-8"?>
  <resources>
      <usb-device class="8" subclass="6" protocol="80"/>
  </resources>

  ── ИТОГОВЫЙ МАНИФЕСТ после правок выглядит так ────────────────────────────
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature android:name="android.hardware.usb.host" android:required="false"/>
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"
        tools:ignore="ForegroundServicesPolicy" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.ModuleSKZI"
        tools:ignore="DataExtractionRules">

        <activity android:name="org.pniei.moduleskzi.activities.LauncherActivity"
            android:screenOrientation="fullSensor"
            android:exported="true"
            tools:ignore="DiscouragedApi">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED"/>
            </intent-filter>
            <meta-data
                android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED"
                android:resource="@xml/usb_device_filter"/>
        </activity>

        <activity
            android:name="org.pniei.moduleskzi.activities.MainActivity"
            android:screenOrientation="fullSensor"
            android:exported="true"
            tools:ignore="DiscouragedApi">
        </activity>
        <activity android:name="org.pniei.moduleskzi.activities.SecondaryActivity"
            android:screenOrientation="fullSensor"
            android:windowSoftInputMode="stateHidden|adjustResize"
            tools:ignore="DiscouragedApi" />
        <activity android:name="org.pniei.moduleskzi.activities.LoginActivity"
            android:screenOrientation="fullSensor"
            android:windowSoftInputMode="stateHidden|adjustResize"
            tools:ignore="DiscouragedApi">
        </activity>
        <activity android:name="org.pniei.dwface.biometry.BiometryActivity"
            android:screenOrientation="fullSensor"
            android:windowSoftInputMode="stateHidden|adjustResize"
            tools:ignore="DiscouragedApi">
        </activity>
        <activity android:name="org.pniei.moduleskzi.qrcode.QRCodeScanActivity"
            android:screenOrientation="fullSensor"
            android:windowSoftInputMode="stateVisible"
            tools:ignore="DiscouragedApi">
        </activity>

        <service android:name="org.pniei.portal.vpn.VpnService"
            android:permission="android.permission.BIND_VPN_SERVICE"
            android:foregroundServiceType="dataSync"
            android:exported="false"
            tools:ignore="VpnServicePolicy">
            <intent-filter>
                <action android:name="android.net.VpnService"/>
            </intent-filter>
        </service>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths"/>
        </provider>
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:mimeType="*/*"/>
        </intent>
    </queries>
</manifest>
